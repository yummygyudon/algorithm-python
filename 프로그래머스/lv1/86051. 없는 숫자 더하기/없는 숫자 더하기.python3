def solution(numbers):
    # numbers.sort()
    # ans = 0
    # idx = 0
    # for i in range(10) :
    #     if numbers[idx] == i :
    #         idx += 1
    #     else :
    #         ans += i
    # return ans
    ans =0
    for i in range(10) :
        if i not in numbers :
            ans += i
    return ans